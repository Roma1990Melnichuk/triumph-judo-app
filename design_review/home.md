---
screen: home
status: partial
---

## Головна — ⚠️ Частково відповідає

**Макет:** Variant_1, "HOME (Coach)" — опис у DESIGN CONTEXT; var_0.png позиція 4 "Головна тренера"

### Що відповідає макету ✅
- AppBar-рядок: привітання зліва ("Доброго ранку, [Ім'я]") + іконка дзвоника справа з червоною крапкою — точно відповідає макету
- Hero-картка: градієнт `#7A0000 → #D50000 → #FF8A00` (135°), кутовий радіус 20px, тінь з `primary.withValues(alpha:0.35)` — кольори та форма відповідають
- Рядок статистики: 3 картки — Спортсменів / Відвідуваність % / Медалі — відповідає макету (макет: Спортсмени / Відвідуваність / Заходи; цифри динамічні)
- Темний фон `AppColors.background` (#050000) + `AmbientBackground` — відповідає кольоровій схемі
- Секція "Останні досягнення" з іконками медалей і списком — присутня
- Анімації входу (fade + translate) — покращення UX, не суперечать макету
- Картка членства (`AcademyPassCard` для батьків / `TeamMembershipCard` для тренера) — відповідає секції "Мій абонемент" з макету Variant_12

### Що відрізняється від макету ⚠️ / ❌
- **Hero-картка — назва:** макет показує `"Активне тренування"` як заголовок; код виводить `"Наступне тренування"` — назва інша
- **Hero-картка — статистика учнів:** макет містить лічильник `"12 учнів"` прямо на Hero-картці; у коді цього поля немає — показано лише час та дні
- **Hero-картка — play-кнопка:** макет має великий play-button-style елемент; код використовує `TIcon.training` у квадратному контейнері 58×58 — стилістично інше
- **Статистика — третя картка:** макет показує `"Заходи"` (23); код показує `"Медалі"` (з `totalResultsCountProvider`) — різні метрики
- **Секція "Останні досягнення":** макет показує badge thumbnails у горизонтальному скролі; код реалізує вертикальний список (`ListTile` у `Column`) — принципово інший UI-патерн
- **Секція belt-ready (`_BeltReadySection`):** відсутня у макеті тренера — додатковий елемент, не передбачений дизайном
- **`MedalTrackerDashboardCard`:** відсутня у макеті — нестандартний додатковий блок
- **Для батьків — 4 іконки швидкого доступу (`_ParentFeatureIcons`):** макет Variant_12 передбачає цей ряд, але точний склад іконок потребує звірки з оригінальним PNG
- **Для батьків — `JourneyHomeWidget`:** відповідає макету Journey (Variant_12), але розміщений на головній — потрібно звірити, чи це передбачено окремим екраном чи вбудованим віджетом

### Критичні розбіжності
- **Горизонтальний скрол досягнень** (badge thumbnails) замінений вертикальним списком результатів змагань — суттєва відмінність від задуму дизайнера; потрібно переробити `_RecentSection` на горизонтальний `ListView` з badge-картками
- **Лічильник учнів на Hero-картці** відсутній — видима метрика, яку дизайнер виділив як ключову; потрібно додати до `_HeroCard`
